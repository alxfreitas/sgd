package sgd

class AcessoFilters {

    def filters = {

        /*
        all(controller: 'base', action: 'home') {
            before = {
                if(!session.usuario && !actionName.equals('login')){
                    redirect(controller: 'usuario', action: 'login')
                    return false
                }
                else {
                    return true
                }


            }
            after = { Map model ->

            }
            afterView = { Exception e ->

            }

        }
        */



    }
}
